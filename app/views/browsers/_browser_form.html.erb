<%= form_for(@browser) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label 'Genome fetcher link' %>
  <%= f.text_field :genome %>

  <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
<% end %>